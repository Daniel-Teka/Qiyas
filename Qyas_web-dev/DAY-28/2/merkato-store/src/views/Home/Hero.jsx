export default function Hero({ setView }) {
   return (
       <section className="hero">
             <h1>Welcome to Merkato Store</h1>
                   <p>Modern shopping for Africa and the Middle East.</p>
                         <button onClick={() => setView('products')}>Shop Now</button>
                             </section>
                               );
                               }
